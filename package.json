{
  "name": "keepcalm-pdf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "mkdir -p ./.cache && mv /app/.cache/puppeteer ./.cache || true",
    "test": "echo \"no tests\""
  },
  "dependencies": {
    "puppeteer": "^24.32.1"
  }
}
